name: MergeAgent Theme Park Steward probe

on:
  push:
    branches:
      - integration/theme-park-growth-consolidation-20260912

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: integration/theme-park-growth-consolidation-20260912
          fetch-depth: 0
      - name: Probe exact Steward stack
        shell: bash
        run: |
          set -u
          git config user.name 'AXM MergeAgent'
          git config user.email 'actions@users.noreply.github.com'
          set +e
          git merge --no-commit --no-ff c33edbb476840ed1bdbdeed9c13c7812e3dafcf7
          status=$?
          set -e
          if [ "$status" -ne 0 ]; then
            echo '--- UNMERGED FILES ---'
            git diff --name-only --diff-filter=U
            echo '--- STATUS ---'
            git status --short
            exit 1
          fi
          echo 'MERGE_CLEAN'
