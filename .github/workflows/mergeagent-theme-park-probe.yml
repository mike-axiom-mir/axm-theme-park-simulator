name: MergeAgent Theme Park save-recovery probe

on:
  push:
    branches:
      - integration/theme-park-growth-consolidation-20260912

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: integration/theme-park-growth-consolidation-20260912
          fetch-depth: 0
      - name: Probe exact save-integrity/recovery terminal
        shell: bash
        run: |
          set -u
          git config user.name 'AXM MergeAgent'
          git config user.email 'actions@users.noreply.github.com'
          set +e
          git merge --no-commit --no-ff ccf565986fe1f37338d83d08fe1cda4aaa7efef2
          status=$?
          set -e
          if [ "$status" -ne 0 ]; then
            echo '--- UNMERGED FILES ---'
            git diff --name-only --diff-filter=U
            echo '--- STATUS ---'
            git status --short
            exit 1
          fi
          echo 'MERGE_CLEAN'
