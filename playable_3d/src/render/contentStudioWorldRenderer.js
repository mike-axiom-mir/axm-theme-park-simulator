/**
 * Compatibility seam for the active playable lineage.
 *
 * Keep main.js importing this stable filename while the presentation stack grows
 * additively underneath it. The final layer adds visible installed-upgrade
 * hardware and player-selectable district styling; simulation authority remains
 * outside render code.
 */
export { WorldRenderer } from "./stateSafeParkIdentityWorldRenderer.js";
