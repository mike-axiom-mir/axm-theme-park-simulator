# Changelog

## v0.3.0 — Runtime Integration and Living-Park Reference

- preserved the approved v0.2.0 canonical root hash;
- retained the 0.2.0 save envelope while advancing additive contracts to 0.3.0;
- added deterministic scheduler and simulation/render cadence rules;
- added state namespace ownership and atomic commit/fork model;
- added auditable action proposal/decision/event routing;
- added atomic command/event/reducer kernel and duplicate/stale rejection;
- added verified snapshots, restore, and branch lineage;
- added conserved audience cohorts with first/repeat demand and capacity limits;
- added causal contribution ledger with split attribution and anti-double-counting;
- added metric/evidence provenance and counterfactual review;
- added rebuildable non-authoritative animation signals;
- added runtime assembly planner, return packet verifier, and acceptance gate;
- added language-neutral schemas and beginner API map;
- added four-demo deterministic reference suite and two living-park slices;
- hardened finite-number input boundaries, JSON-safe animation parameters,
  prior-exposure-preserving visitor recovery, deterministic ledger identity, and
  counterfactual hash/duplicate validation;
- expanded validation to 136 passing tests before archive verification;
- embedded the complete v0.2.0 ZIP for rollback.

## v0.2.0

- expanded deterministic world, evidence, identity, campaign, save, merge, and
  presentation foundation;
- 42 tests passed.

## v0.1.0

- initial deterministic theme-park foundation.
